# Ảnh dự án

Ảnh hiện tại là **placeholder** (picsum.photos + SVG tên thương hiệu) để test layout.

Thay bằng ảnh thật khi deploy — giữ đúng tên file hoặc cập nhật path trong view/DB.

Chạy lại script tải mẫu: `powershell -File scripts/download-placeholders.ps1`
